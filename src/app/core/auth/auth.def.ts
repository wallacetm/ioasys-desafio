/**
 * @typedef LoginInput
 * @property {string} email User's email.
 * @property {string} password User's password.
 */

/**
 * @typedef LoginOutput
 * @property {string} token Token to add in authorization headers.
 * @property {string} tokenType Token type to add in authorization headers.
 */